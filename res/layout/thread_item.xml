<?xml version="1.0" encoding="utf-8"?>
<!-- For home threads -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:paddingTop="10dp"
              android:paddingRight="10dp"
              android:paddingBottom="14dp"
              android:paddingLeft="8dp"
              android:orientation="vertical" >

  <TextView
      android:layout_width="0dp"
      android:layout_height="0dp"
      android:id="@+id/thread_nid"
      android:visibility="gone" />
  
  
  <RelativeLayout
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="5dp"
      android:orientation="horizontal" >

    <TextView
        android:id="@+id/thread_node"
        android:onClick="onNodeClick"
        android:clickable="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Node"
        android:textColor="#2BA6CB"
        android:textSize="15sp" />
    <TextView
        android:id="@+id/thread_split"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/thread_node"
        android:layout_marginRight="5dp"
        android:text="-"
        android:textColor="#111" />
    <TextView
        android:id="@+id/thread_thread"
        android:onClick="onThreadClick"
        android:clickable="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/thread_split"
        android:text="Thread"
        android:textColor="#2BA6CB"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/thread_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:text="Time time time"
        android:textColor="#999"
        android:textSize="12sp" />
  </RelativeLayout>

  <TextView
      android:id="@+id/thread_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="5dp"
      android:text="Title"
      android:textColor="#333"
      android:textSize="15sp" />

  <TextView
      android:id="@+id/thread_body"
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:layout_marginBottom="0dp"
      android:text="Content"
      android:textColor="#666"
      android:textSize="13sp" />

</LinearLayout>
